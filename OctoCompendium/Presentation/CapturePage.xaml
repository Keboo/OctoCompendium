<Page x:Class="OctoCompendium.Presentation.CapturePage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:local="using:OctoCompendium.Presentation"
      xmlns:utu="using:Uno.Toolkit.UI"
      Background="{ThemeResource BackgroundBrush}">

  <Grid utu:SafeArea.Insets="VisibleBounds">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto" />
      <RowDefinition />
      <RowDefinition Height="Auto" />
    </Grid.RowDefinitions>

    <utu:NavigationBar Content="Scan Sticker" />

    <StackPanel Grid.Row="1"
                HorizontalAlignment="Center"
                VerticalAlignment="Center"
                Spacing="24">
      <TextBlock Text="🔍" FontSize="72" HorizontalAlignment="Center" />
      <TextBlock Text="{Binding StatusMessage}"
                 TextWrapping="Wrap"
                 HorizontalAlignment="Center"
                 TextAlignment="Center" />
      <ProgressRing IsActive="{Binding IsProcessing}"
                    Height="48" Width="48"
                    HorizontalAlignment="Center" />

      <!-- Model download UI -->
      <StackPanel Visibility="{Binding IsModelMissing}"
                  Spacing="12"
                  HorizontalAlignment="Center">
        <ProgressBar Value="{Binding DownloadProgress}"
                     Maximum="1"
                     Visibility="{Binding IsDownloading}"
                     HorizontalAlignment="Stretch"
                     MinWidth="240" />
        <Button Content="⬇️ Download CLIP Model"
                Command="{Binding DownloadModelCommand}"
                HorizontalAlignment="Center"
                Style="{StaticResource FilledButtonStyle}" />
      </StackPanel>
    </StackPanel>

    <Button Grid.Row="2"
            Content="📷 Choose Photo"
            Command="{Binding PickPhotoCommand}"
            HorizontalAlignment="Stretch"
            Margin="16"
            Style="{StaticResource FilledButtonStyle}" />
  </Grid>
</Page>
