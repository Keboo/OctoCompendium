<Page x:Class="OctoCompendium.Presentation.MainPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:local="using:OctoCompendium.Presentation"
      xmlns:uen="using:Uno.Extensions.Navigation.UI"
      xmlns:utu="using:Uno.Toolkit.UI"
      xmlns:um="using:Uno.Material"
      NavigationCacheMode="Required"
      Background="{ThemeResource BackgroundBrush}">

  <Grid utu:SafeArea.Insets="VisibleBounds">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" />
      <RowDefinition />
      <RowDefinition Height="Auto" />
    </Grid.RowDefinitions>

    <!-- Header -->
    <utu:NavigationBar Content="OctoCompendium" />

    <!-- Stats bar -->
    <StackPanel Grid.Row="1" Orientation="Horizontal" Padding="16,8" Spacing="12">
      <TextBlock>
        <Run Text="{Binding OwnedCount}" />
        <Run Text=" / " />
        <Run Text="{Binding TotalCount}" />
        <Run Text=" collected" />
      </TextBlock>
      <ProgressBar Minimum="0" Maximum="{Binding TotalCount}" Value="{Binding OwnedCount}"
                   Width="150" VerticalAlignment="Center" />
    </StackPanel>

    <!-- Sticker grid -->
    <GridView Grid.Row="2"
              ItemsSource="{Binding Stickers}"
              Padding="8">
      <GridView.ItemTemplate>
        <DataTemplate>
          <Grid Width="100" Height="120" Margin="4"
                Opacity="{Binding IsOwned, Converter={StaticResource BoolToOpacityConverter}}">
            <Grid.RowDefinitions>
              <RowDefinition />
              <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <Border Background="{ThemeResource SurfaceVariantBrush}"
                    CornerRadius="8">
              <TextBlock Text="🏷️" FontSize="36"
                         HorizontalAlignment="Center" VerticalAlignment="Center" />
            </Border>
            <TextBlock Grid.Row="1" Text="{Binding Name}"
                       TextTrimming="CharacterEllipsis"
                       HorizontalAlignment="Center"
                       FontSize="11" Margin="0,4,0,0" />
          </Grid>
        </DataTemplate>
      </GridView.ItemTemplate>
    </GridView>

    <!-- Scan button -->
    <Button Grid.Row="3"
            Content="📸 Scan Sticker"
            Command="{Binding ScanSticker}"
            HorizontalAlignment="Stretch"
            Margin="16"
            Style="{StaticResource FilledButtonStyle}" />
  </Grid>
</Page>
